// auto-stub
'use client'
import React from 'react'

export const TeamWorkflowDependencyGraph = React.forwardRef<HTMLElement, any>(
  ({className='',children,...p},ref) => React.createElement('div',{ref,className,...p},children)
)
TeamWorkflowDependencyGraph.displayName = 'TeamWorkflowDependencyGraph'

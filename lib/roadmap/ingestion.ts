import { RoadmapItem } from "./roadmap-loader";
export interface IngestionResult {
    // Task title (starts with ## or -)
      // Save previous task
      // Start new task
    // Description
  // Save last task
export default {}
